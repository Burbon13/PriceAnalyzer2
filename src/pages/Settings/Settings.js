import React from "react";
import useStyles from "./styles";

const Settings = () => {
    const classes = useStyles();

    return (
        <div>
            Settings
        </div>
    );
};

export default Settings;
