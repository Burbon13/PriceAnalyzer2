import React from "react";
import useStyles from "./styles";

const Info = () => {
    const classes = useStyles();

    return (
        <div>
            Info
        </div>
    );
};

export default Info;
